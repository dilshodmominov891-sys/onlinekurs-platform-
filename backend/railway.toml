[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn -w 1 --threads 100 -b 0.0.0.0:$PORT app:app"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
